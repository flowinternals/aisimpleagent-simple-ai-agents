/**
 * Single source of truth for POST /api/generate prompt length (UI + server).
 * Imported by `src/App.tsx` and `server/validation/generationRequest.js`.
 */
export const GENERATION_PROMPT_MAX_LENGTH = 2000;
